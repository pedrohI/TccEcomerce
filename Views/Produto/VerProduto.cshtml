<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Produto</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
       background-color: #D9ECF7;
    }
    .product-container {
      max-width: 1000px;
      margin: auto;
      padding: 2rem;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0,0,0,0.05);
    }
    .product-image {
      width: 100%;
      border-radius: 10px;
    }
    .frete-result {
      font-size: 0.9rem;
      color: #555;
    }
  </style>
</head>
<body>

<div class="container my-5">
  <div class="product-container row g-4">
    
    <!-- Imagem -->
    <div class="col-md-6">
      <img src="https://images.tcdn.com.br/img/img_prod/1023561/teste_importacao_23_1_c0e0e4ffb489ba74ed2cd344efe086c4_20230724113241.jpg" class="product-image" alt="Produto">
    </div>

    <!-- Informações -->
    <div class="col-md-6">
      <h2 class="mb-3">Nome do produto</h2>
      <h3 class="text-success mb-3">R$ 2.499,00</h3>
      <p class="mb-1"><strong>Estoque:</strong> Disponível</p>

      <!-- Frete -->
      <div class="my-4">
        <h6>Calcular frete</h6>
        <div class="input-group mb-2" style="max-width: 300px;">
          <input type="text" id="cep" class="form-control" placeholder="Digite seu CEP">
          <button class="btn btn-outline-primary" onclick="calcularFrete()">Calcular</button>
        </div>
        <div id="freteResultado" class="frete-result"></div>
      </div>

      <!-- Comprar -->
      <a asp-controller="Produto" asp-action="Checkout" class="btn btn-primary btn-lg mt-3">Comprar agora</a>
    </div>

    <!-- Descrição -->
    <div class="col-12 mt-4">
      <h4>Descrição do Produto</h4>
      <p>
        O Smartphone Galaxy XYZ conta com uma tela AMOLED de 6.5", câmera tripla de 64MP e 128GB de armazenamento interno.
        Ideal para quem busca desempenho, design moderno e excelente custo-benefício.
        Compatível com 5G, leitor digital na tela e bateria de longa duração.
      </p>
    </div>
  </div>
</div>

<script>
  function calcularFrete() {
    const cep = document.getElementById('cep').value;
    const resultado = document.getElementById('freteResultado');

    if (!cep || cep.length < 8) {
      resultado.innerText = "Por favor, insira um CEP válido.";
      return;
    }

    // Simulação de frete fixo
    resultado.innerText = "Frete para " + cep + ": R$ 19,90 (PAC - 5 dias úteis)";
  }
</script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
